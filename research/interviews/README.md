# Interviews

One file per call: `<YYYY-MM-DD>-<persona>-<slug>.md`.

Raw transcripts (if any) go in `_raw/` (gitignored if sensitive; otherwise committed).

Use the `ingest-interview` skill to process new calls — don't freestyle.
